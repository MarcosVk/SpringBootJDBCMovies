app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/movies
      password: 'Vviiggnneesshh'
      pool-size: 30
      username: vicky

server:
  error:
    include-binding-errors: always
    include-message: always


